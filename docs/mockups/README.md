# Mockups

Salidas de Claude Design, antes de portarlas al código.

Flujo: brief (`docs/BRIEF-DESIGN.md`) → Claude Design → `.html` acá → Claude Code lo porta a `js/modules/` + `css/components.css`.

Los mockups NO se mantienen actualizados. Son referencia visual del momento en que se diseñó cada pantalla, no fuente de verdad. La fuente de verdad es el código.
